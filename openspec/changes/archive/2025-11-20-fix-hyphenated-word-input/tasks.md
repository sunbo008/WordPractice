# 实现任务清单

## 1. 代码修改
- [x] 1.1 修改 `WordTetrisGame.js` 中的 `bindEvents()` 方法，允许输入连字符
  - 更新字母输入处理逻辑（第 642 行）：支持连字符键入
  - 更新 input 事件过滤器（第 665 行）：允许连字符字符
- [x] 1.2 修改 `handleLetterInput()` 方法，支持连字符输入
  - 将方法重命名为 `handleCharacterInput()` 以更准确反映功能
  - 更新输入长度限制逻辑，考虑连字符占位
- [x] 1.3 验证 `vocabularyManager.checkAnswer()` 是否正确处理带连字符的单词
  - 检查字符串比较逻辑
  - 确保大小写不敏感比较
- [x] 1.4 更新实时显示逻辑 `updateRealTimeDisplay()`，确保连字符正确显示

## 2. 测试验证
- [x] 2.1 创建测试用例：测试 `pencil-box` 输入
- [x] 2.2 创建测试用例：测试 `well-known` 输入
- [x] 2.3 创建测试用例：验证连字符位置不影响缺失字母逻辑
- [x] 2.4 回归测试：确保普通单词输入不受影响

## 3. 文档更新
- [x] 3.1 更新设计文档，说明支持连字符输入
- [x] 3.2 更新测试指南，添加连字符单词测试说明
